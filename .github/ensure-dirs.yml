name: Setup Runtime Directories

# Triggers: manual dispatch or any push to main
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths-ignore:
      - 'log/**'
      - 'answers/**'

jobs:
  ensure-dirs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Ensure log/ and answers/ directories exist
        run: |
          mkdir -p log answers
          touch log/.gitkeep answers/.gitkeep

      - name: Commit if directories were missing
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add log/.gitkeep answers/.gitkeep
          if git diff --cached --quiet; then
            echo "Nothing to commit — directories already present."
          else
            git commit -m "chore: ensure log/ and answers/ runtime directories exist [skip ci]"
            git push
          fi
